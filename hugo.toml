# ==========================================================
# 1. 다국어 설정 (최종 완성 버전)
# ==========================================================
baseURL = "https://heavyrain39.github.io/archive03/" # [추가] 사이트의 기본 URL. 가장 중요한 설정입니다.

defaultContentLanguage = 'ko'
resourceDir = "tmp/hugo-resources"

[languages]
  [languages.ko]
    languageName = '한국어'
    weight = 1
    title = 'Archive 03'
    [languages.ko.outputs]
      home = ["HTML", "JSON", "RSS"]
      section = ["HTML", "RSS"]
      page = ["HTML"]
    [languages.ko.params]
      subtitle = '세 명의 인외 소녀가 전하는 투자 인사이트'
      description = "Archive 03은 세 명의 인외(人外) 소녀가 최신 데이터를 바탕으로 투자에 대한 인사이트를 전하는 연재형 블로그입니다."
      dateFormat = "2006년 1월 2일"
    [[languages.ko.menu.main]]
      identifier = "article"
      name = "HOME"
      url = "/"
      weight = 10
    [[languages.ko.menu.main]]
      identifier = "about"
      name = "ABOUT"
      pageRef = "/about.ko.md"
      weight = 20

  [languages.en]
    languageName = 'English'
    weight = 2
    title = 'Archive 03'
    [languages.en.outputs]
      home = ["HTML", "JSON", "RSS"]
      section = ["HTML", "RSS"]
      page = ["HTML"]
    [languages.en.params]
      subtitle = 'Financial Insights from 3 Non-human Girls'
      description = "Archive 03 is a serial blog where three non-human girls provide investment insights based on the latest data."
      dateFormat = "January 2, 2006"
    [[languages.en.menu.main]]
      identifier = "article"
      name = "HOME"
      url = "/en/"
      weight = 10
    [[languages.en.menu.main]]
      identifier = "about"
      name = "ABOUT"
      pageRef = "/about.en.md"
      weight = 20

# ==========================================================
# 2. URL 구조 설정
# 연도별 폴더로 콘텐츠를 정리해도 공개 URL은 기존처럼 파일명만 사용합니다.
# 예: content/reports/2026/foo.ko.md -> /reports/foo/
[permalinks]
  [permalinks.page]
    reports = '/reports/:contentbasename/'

# ==========================================================
# 3. 기존 설정 (일부 삭제됨)
# ==========================================================
timeZone = "Asia/Seoul"

[pagination]
  pagerSize = 5

[params]
  author = '야차완'
  description = "Archive 03: 세 명의 인외 소녀가 전하는 투자 인사이트"
  favicon = "/favicon.ico"
  
  [params.giscus]
    repo = "heavyrain39/archive03"
    repoId = "R_kgDOO7KqJQ"
    category = "General"
    categoryId = "DIC_kwDOO7KqJc4CrlEO"
    mapping = "pathname"
    strict = "0"
    reactionsEnabled = "0"
    emitMetadata = "0"
    inputPosition = "bottom"
    theme = "dark_dimmed" 
    loading = "lazy"

# Goldmark 설정
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.highlight]
    style = "monokai"

# 미디어 타입 및 출력 포맷 설정
[mediaTypes]
  [mediaTypes."text/css"]
    suffixes = ["css"]

[outputFormats.JSON]
  baseName = "index"
  isPlainText = true
  mediaType = "application/json"
